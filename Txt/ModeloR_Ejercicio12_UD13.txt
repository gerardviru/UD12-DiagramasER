Empresa (CIF(pk int), DNI, Nombre, Direcci贸n, Tel茅fono, C贸digoInterno)

Proyecto (IdProyecto(pk int), FechaInicio, FechaProvisional, FechaFinal)

Crear (CIF(pk int)(fk int), IdProyecto(pk int)(fk int) )

Trabajador (DNI(pk int), Nombre, Apellidos, C贸digoTrabajador, HorasTrabajadas)

Trabajar (IdProyecto(pk int)(fk int), DNI(pk int)(fk int) )

Profesi贸n (IdProfesi贸n(pk int), DNI((fk int) Nombre)

/******************************************************/

Edit:

Empresa (Codigo interno empresa(pk String), CIF, Nombre, Direccion, Telefono)

Proyecto (IdProyecto(pk int), CodigoInternoEmpresa(fk String) FechaInicio, FechaProvisional, FechaFinalzado)

Trabajador (CodigoTrabajador(pk String), Nombre, Apellidos, DNI)

Trabajar (IdProyecto(pk/fk int), CodigoTrabajador(pk/fk String), Horas Trabajadas )

Profesion (CodigoProfesion(pk String), Nombre, Descripci髇)

Ejercer (CodigoProfesion(pk/fk String), CodigoTrabajador (pk/fk int) )
